# Backlog refresh — 2026-05-01

**Repo:** `D:\RAG_Support_Assistant`
**Branch:** `master`
**Baseline:** `ebbacb5 test: make regression eval ci deterministic`
**Status:** active backlog source after the GraceKelly default-provider sync.

## Project Status

The project is not blocked by the old Phase 2k / router split notes. The
router/app split, coverage gate, Helm secret split, streaming parity, uv locks,
security CI, thin app-shell, strict mypy scope, regression-eval CI gate, and
GraceKelly-primary default provider alignment are closed in the current history.
`docs/plans/2026-04-27-next-steps.md` is complete for steps 1-9 and is now a
historical execution plan, not the next backlog.

`codex-tasks/ROADMAP.md` is also historical for arc 102-122. Its only live
item is explicitly non-blocking: post-production resolution-rate targets.

## Requires Explicit Opt-In

1. **Live Batch N benchmark.** Batch M closed the last operational Known Gap.
   The only remaining benchmark candidates are GraceKelly full-RAG-stack e2e
   and Mistral regression benchmark. Both require explicit user opt-in for
   live external-provider/runtime quota.

## Recently Closed

- Documentation truth sync: this file, `codex-tasks/ROADMAP.md`, README,
  Quickstart, runbook, and changelog now point at GraceKelly-primary as the
  default runtime path.
- Regression eval CI gate wiring: the PR job runs
  `--mock-experiment-runtime --no-persist`, does not persist to the database,
  and no longer sets `continue-on-error`.
- Provider readiness sync: `/api/health` checks active LLM providers from the
  selected routing profile instead of always requiring Ollama.
- Tool-call observability: `tracing/langfuse_trace.py` includes `tool_calls`
  metadata for both Langfuse `start_observation` and legacy `trace.generation`
  paths, and `agent/graph.py` now forwards provider `generate_with_tools`
  tool-call turns into `trace_llm_call`.
- Agent Copilot UI/context polish: `static/agent.html` renders
  `retrieved_docs` and `quality_scores`, and similar ticket results now omit
  zero-overlap resolved fillers.
- Batch N safe mock run: `python scripts/regression_eval.py --baseline
  ollama-small --candidate mistral-small-latest --dataset
  evaluation/curated_cases.jsonl --tenant all --max-cases 5 --seed 42
  --no-persist` completed in `mock-provider-benchmark` mode with gate pass and
  no live external-provider opt-in.
- A11y widget coverage: `static/widget.html` is now included in static a11y
  coverage and has one explicit `<main>` landmark.
- A11y/performance verification is closed: `@axe-core/cli` 4.11.3 produced
  `38 passed` in `tests/test_a11y.py`, and Lighthouse mobile
  `/static/chat.html` scored performance `99`, accessibility `100`,
  best-practices `96`, SEO `90`.
- Colab remote benchmark setup is closed on `master`: the runbook and notebook
  are committed, PR #1 was merged, and local/iMac machines are documented as
  thin clients.

## Closed But Stale In Older Docs

- Batch F task-141/142 are closed by `d71507f` and `c7a49fd`.
- Batch I task-154/task-156 are closed by `da94ed3`.
- Task 177/178/179 regression plumbing is closed on the RAG side; the remaining
  GraceKelly rerun/e2e work is optional benchmark work, not task-177 plumbing.
- `commercial-upgrade-plan.md` and `codex-tasks/verification-report.md` are
  historical snapshots and should not be used as active backlog.

## Deferred / Optional

- Production resolution rate >=50%: requires real traffic.
- Backup key rotation automation: documented manual runbook exists.

## Parallel Lanes

| Lane | Owns | Avoids |
| --- | --- | --- |
| Benchmark | `scripts/regression_eval.py`, `evaluation/curated_cases.jsonl`, `docs/QUICKSTART.md`, `reports/regression/*` | Live external-provider calls without opt-in |

## Short Backlog

Current non-live autopilot-safe backlog: none. Do not reopen the top-level
`AP-1` / `AP-2` housekeeping tasks; `test: guard historical backlog pointers`
and `docs: refresh autopilot state snapshot` are already closed.

2026-05-30 note: the Colab remote benchmark setup is also closed. Current
branch/handoff state lives in `AGENT_STATE.md` and `next-session-3-subagents.md`.

1. **CLOSED 2026-05-07 — Live Batch N benchmark decision.** Decision: do not
   schedule a live GraceKelly+Mistral regression as a routine gate. The
   mock-provider-benchmark run logged in *Recently Closed* (gate pass on
   `ollama-small` vs `mistral-small-latest`, 5 cases, `--no-persist`,
   `mock-experiment-runtime`) is the canonical regression signal; CI also
   exercises `--mock-experiment-runtime --no-persist` on every PR without
   `continue-on-error`. A live run remains available as a discretionary
   experiment, not a backlog item — invoke it only when there is a specific
   business reason (e.g. validating a candidate model swap before rollout) and
   the runtime is staged.
   - 2026-05-07 live attempt is documented in history below for posterity, but
     does not reopen the backlog: the run was aborted at provider init because
     Ollama was not serving on localhost:11434 and `MISTRAL_API_KEY` was in
     `.env` but not in the subprocess environment, so `regression_eval.py`
     exited 0 with a graceful error envelope, no live provider calls were made,
     and no benchmark report was produced.
   - 2026-05-30 live readiness check: `MISTRAL_API_KEY` is present in `.env`
     and `GET https://api.mistral.ai/v1/models` returned `200`; GraceKelly was
     not reachable on `http://127.0.0.1:8011/healthz/ready`. No live
     GraceKelly+Mistral benchmark was run because starting GraceKelly, Docker,
     or local services is outside the current local resource boundary.
   - 2026-05-30 explicit live opt-in follow-up: GraceKelly was started on
     `http://127.0.0.1:8011`, readiness/model catalog/orchestrate smoke passed,
     and a direct Mistral live regression exposed that the default local
     `rag_docs_default` Chroma collection was stale/incompatible
     (dimension 3 vs `BAAI/bge-m3` 1024). Commit `7b0d9ee` now fails closed on
     that condition. A separate ignored eval collection
     `rag_eval_20260530t0835_default` built from the tracked demo KB docs
     passed a 3-case live Mistral run
     (`ministral-3b-latest` vs `mistral-small-latest`, 100%/100%, 0
     regressions, 0 infrastructure failures). This does not reopen the closed
     Batch N decision; it is partial R7 evidence only.
   - 2026-05-30 R3/R4 fan-out follow-up: commit `71367a7` batches
     multi-document `grade_docs` into one structured LLM call, with JSON/text
     parsing fallback and the previous per-doc path retained when batch grading
     is unavailable. Master CI run `26679982808` and Pages run `26679982810`
     passed. This reduces the documented per-doc grade fan-out; larger-latency
     proof should ride on the staged R7 eval set rather than another tiny smoke.
   - 2026-05-30 R4 observability follow-up: commit `c0b6d24` adds trace events
     with durations for `verify_facts.extract_claims` and
     `verify_facts.verify_claim`, so the claim fan-out is measurable from
     traces. Master CI run `26680293620` and Pages run `26680293609` passed.
   - Rerun recipe (manual, explicit opt-in only — runs a live external-provider
     benchmark; do not invoke without a concrete business reason and staged
     runtime):
     start Ollama (`ollama serve` plus `ollama pull` for the `ollama-small`
     alias resolved from `config/providers.yml`), export `.env` keys into the
     shell (e.g. `set -a; source .env; set +a` in bash), then re-run:
     `python scripts/regression_eval.py --baseline ollama-small --candidate
     mistral-small-latest --dataset evaluation/curated_cases.jsonl --tenant
     all --allow-paid-apis --seed 42` — live, manual opt-in only.

## Parallel Session Prompts

### Prompt 1 — Live Batch N benchmark decision

```text
Repo: D:\RAG_Support_Assistant
Branch: master
Start read-only. Respect AGENTS.md.

Task: decide whether to request explicit opt-in for the remaining live Batch N
benchmark. Do not run live external-provider benchmark commands unless the user
opts in during this session.

Own only:
- docs/plans/2026-05-01-backlog.md
- next-session-3-subagents.md

Goal:
- Leave mock/default benchmark docs and guardrails closed.
- Keep GraceKelly/Mistral live benchmark work explicit opt-in/manual only.
- Without explicit opt-in, stop after read-only decision notes.

Workflow:
1. Inspect the active backlog and existing regression reports.
2. If no explicit live opt-in is given, do not run benchmark commands and do
   not reopen mock guardrail tasks.
3. If explicit opt-in is given, verify env/service readiness before any live
   command and report expected cost/runtime.

Report whether a live benchmark ran, the commands run if any, and any residual
limitation.
```
