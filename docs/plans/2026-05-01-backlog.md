# Backlog refresh — 2026-05-01

**Repo:** `D:\RAG_Support_Assistant`  
**Branch:** `master`  
**Baseline:** `ebbacb5 test: make regression eval ci deterministic`
**Status:** active backlog source after the GraceKelly default-provider sync.

## Project Status

The project is not blocked by the old Phase 2k / router split notes. The
router/app split, coverage gate, Helm secret split, streaming parity, uv locks,
security CI, thin app-shell, strict mypy scope, regression-eval CI gate, and
GraceKelly-primary default provider alignment are closed in the current history.
`docs/plans/2026-04-27-next-steps.md` is complete for steps 1-9 and is now a
historical execution plan, not the next backlog.

`codex-tasks/ROADMAP.md` is also historical for arc 102-122. Its only live
items are explicitly non-blocking: Lighthouse/performance measurement and
post-production resolution-rate targets.

## Actually Open

1. **Agent Copilot context quality.** `api/routers/agent.py` still returns
   empty `retrieved_docs` and `quality_scores`, and "similar tickets" are the
   latest resolved tickets rather than semantic matches.
2. **Tool-call observability.** `agent/graph.py` records `tool_calls`, but
   `tracing/langfuse_trace.py` does not send them to Langfuse metadata.
3. **Batch N selection.** Batch M closed the last operational Known Gap.
   Remaining candidates are GraceKelly full-RAG-stack e2e and Mistral
   regression benchmark. Both need explicit opt-in for live service/API cost.

## Recently Closed

- Documentation truth sync: this file, `codex-tasks/ROADMAP.md`, README,
  Quickstart, runbook, and changelog now point at GraceKelly-primary as the
  default runtime path.
- Regression eval CI gate wiring: the PR job runs
  `--mock-experiment-runtime --no-persist`, does not persist to the database,
  and no longer sets `continue-on-error`.
- Provider readiness sync: `/api/health` checks active LLM providers from the
  selected routing profile instead of always requiring Ollama.

## Closed But Stale In Older Docs

- Batch F task-141/142 are closed by `d71507f` and `c7a49fd`.
- Batch I task-154/task-156 are closed by `da94ed3`.
- Task 177/178/179 regression plumbing is closed on the RAG side; the remaining
  GraceKelly rerun/e2e work is optional benchmark work, not task-177 plumbing.
- `commercial-upgrade-plan.md` and `codex-tasks/verification-report.md` are
  historical snapshots and should not be used as active backlog.

## Deferred / Optional

- Lighthouse mobile >=90: performance sweep only.
- Production resolution rate >=50%: requires real traffic.
- Backup key rotation automation: documented manual runbook exists.
- A11y moderate issues: critical/serious axe findings are already zero; moderate
  landmark/region cleanup can be handled as polish.

## Parallel Lanes

| Lane | Owns | Avoids |
| --- | --- | --- |
| Agent Copilot | `api/routers/agent.py`, `tests/test_agent_endpoints.py`, optional `static/agent.html` | `agent/graph.py`, CI |
| Observability | `tracing/langfuse_trace.py`, `tests/test_langfuse_trace.py`, optional `agent/graph.py` | Agent router/UI |
| Benchmark | `scripts/regression_eval.py`, `evaluation/curated_cases.jsonl`, `reports/regression/*` | Live paid/API calls without opt-in |
| A11y polish | `static/*.html`, `templates/*.html`, `tests/test_a11y.py`, `docs/a11y/*` | CI/eval changes unless explicitly scoped |

## Short Backlog

1. **P1 — Agent Copilot semantic context.** Return useful ticket context,
   quality fields, and semantic similar tickets with focused tests.
2. **P1 — Tool-call trace metadata.** Add `tool_calls` to Langfuse generation
   metadata without changing the graph behavior.
3. **P2 — Batch N benchmark decision.** Run only mock/default benchmark flows
   unless the session explicitly opts into live GraceKelly/Mistral cost.
4. **P3 — A11y/performance polish.** Clean moderate landmark/region findings
   and measure Lighthouse mobile score.

## Parallel Session Prompts

### Prompt 1 — Agent Copilot Context

```text
Repo: D:\RAG_Support_Assistant
Branch: master
Start read-only. Respect AGENTS.md.

Task: implement the Agent Copilot context-quality backlog item from
docs/plans/2026-05-01-backlog.md.

Own only:
- api/routers/agent.py
- tests/test_agent_endpoints.py
- static/agent.html only if the API payload requires a tiny UI rendering change

Goal:
- /agent/tickets/{ticket_id} should return non-empty useful context when it exists:
  messages, retrieved_docs, quality_scores.
- /agent/similar should return the 3 most relevant resolved tickets, not just the
  latest 3 by resolved_at. Keep the implementation small and local; do not add a
  new service layer unless tests prove it is needed.

Workflow:
1. Inspect current models/tests and write failing tests first.
2. Implement the smallest change that passes.
3. Run focused tests first, then the relevant non-integration subset.
4. Do not edit docs or CI.

Report changed files, commands run, and any residual limitation.
```

### Prompt 2 — Langfuse Tool-Call Metadata

```text
Repo: D:\RAG_Support_Assistant
Branch: master
Start read-only. Respect AGENTS.md.

Task: implement the tool-call observability backlog item from
docs/plans/2026-05-01-backlog.md.

Own only:
- tracing/langfuse_trace.py
- tests/test_langfuse_trace.py
- agent/graph.py only if needed to pass existing tool_calls into trace_llm_call

Goal:
- Langfuse generation metadata should include tool_calls when the graph has them.
- Existing Langfuse behavior must remain backward compatible for both
  start_observation and legacy trace.generation paths.

Workflow:
1. Write failing tests in tests/test_langfuse_trace.py for metadata containing
   tool_calls.
2. Add the smallest API extension to trace_llm_call.
3. Only touch agent/graph.py if the metadata cannot be supplied otherwise.
4. Run focused tests and ruff on touched files.
5. Do not edit Agent Copilot files or CI.

Report changed files, commands run, and whether agent/graph.py was touched.
```
