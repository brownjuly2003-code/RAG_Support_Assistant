# Task 13 — Research: современный UI для RAG/support-chat приложений (2025)

## Goal
Изучить, какой UI принято делать для корпоративных RAG-чат-ассистентов поддержки в 2025 году.
Найти примеры, паттерны, библиотеки. Зафиксировать рекомендации для нашего проекта.

## Background: текущий UI

Проект уже имеет `static/chat.html` — standalone HTML с тёмной темой, vanilla JS,
без фреймворков. Фичи: chat UI, dark mode toggle, badge'и качества и маршрута,
upload документов, session management. FastAPI отдаёт статику напрямую.

**Вопрос:** стоит ли переходить на полноценный UI-фреймворк (React/Vue/Svelte)?
Или vanilla HTML достаточно для enterprise support assistant?

## Research questions (заполни ответы ниже)

### Q1: Тренды UI для enterprise RAG-ассистентов (2025)

Найди 3-5 открытых проектов или SaaS-продуктов (2024-2025), которые реализуют
support/knowledge-base chat assistant. Для каждого укажи:

**Пример 1:**
```
[Название / URL: ...]
[Stack: React/Vue/vanilla/other?]
[Ключевые UI-паттерны: ...]
[Stars/popularity: ...]
```

**Пример 2:**
```
[Название / URL: ...]
[Stack: ...]
[Ключевые UI-паттерны: ...]
[Stars/popularity: ...]
```

**Пример 3:**
```
[Название / URL: ...]
[Stack: ...]
[Ключевые UI-паттерны: ...]
```

**Вывод по трендам:**
```
[Доминирующий stack в 2025: ...]
[Самые популярные UI-паттерны для RAG-чата: ...]
[Vanilla HTML: всё ещё принято или уже нет? ...]
```

---

### Q2: Ключевые UI-паттерны для RAG support assistant

Найди и опиши следующие паттерны — приняты ли они в 2025, есть ли примеры:

**Source citations в ответе:**
```
[Принято ли показывать источники рядом с ответом? ...]
[Как обычно показывают: inline footnotes / sidebar / expandable / другое?]
[Пример: ...]
```

**Streaming ответов (SSE/WebSocket):**
```
[Насколько это стало нормой в 2025? ...]
[Влияет ли на восприятие качества? ...]
[Реализация: SSE vs WebSocket — что проще для FastAPI?]
```

**Feedback на ответ (👍/👎):**
```
[Насколько важно для RAG-системы поддержки? ...]
[Как данные используются: fine-tuning, routing, logging?]
[Стоит ли добавить в наш проект?]
```

**History / multiple sessions:**
```
[Принято ли хранить историю сессий на стороне UI? ...]
[Паттерн: sidebar с историей vs просто новая сессия?]
```

---

### Q3: Библиотеки / open-source UI компоненты для RAG-чата

Найди готовые open-source компоненты или библиотеки для chat UI (2024-2025):

**Вариант 1 (embedded widget):**
```
[Название: ...]
[URL: ...]
[Плюсы для нашего кейса: ...]
[Минусы: ...]
```

**Вариант 2 (React/Vue компонент):**
```
[Название: ...]
[URL: ...]
[Плюсы: ...]
[Минусы: ...]
```

**Вариант 3 (vanilla / web components):**
```
[Название: ...]
[URL: ...]
[Плюсы: ...]
[Минусы: ...]
```

---

### Q4: Оценка текущего `static/chat.html`

На основе найденных примеров и паттернов:

**Что уже хорошо:**
```
[...]
```

**Чего не хватает (топ-3 самых важных для support-assistant):**
```
1. [feature: ...] — [почему важно для support: ...]
2. [feature: ...] — [почему важно: ...]
3. [feature: ...] — [почему важно: ...]
```

**Стоит ли переходить на React/Vue?**
```
[Рекомендация: да/нет/maybe, аргументы: ...]
[Стоимость миграции vs выгода: ...]
```

---

## Output: Recommendation

```
РЕКОМЕНДАЦИЯ ДЛЯ ПРОЕКТА:

Stack:
[Оставить vanilla HTML / перейти на React / добавить Alpine.js / другое]
Обоснование: [...]

Топ-3 улучшения UI (по соотношению ценность/усилия):
1. [...]
2. [...]
3. [...]

Приоритет для support-ticket сценария:
[...]
```

## CONSTRAINTS
- Только заполнить `[...]` в этом файле
- Сохранить как `docs/research/ui-patterns-2025.md`
- Никаких изменений в коде

## DONE WHEN
- [ ] `docs/research/ui-patterns-2025.md` существует
- [ ] Все `[...]` заполнены
- [ ] Есть чёткая рекомендация: оставить vanilla или перейти на фреймворк
- [ ] Указаны конкретные библиотеки/паттерны с URL
