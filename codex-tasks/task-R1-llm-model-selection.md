# Task R1 — Research: выбор LLM для русскоязычного support-ассистента (2025)

## Goal
Проект использует Ollama/Mistral по умолчанию.
Выяснить: это лучший выбор для русскоязычных support-тикетов, или есть лучше?
Зафиксировать рекомендацию с бенчмарками.

## Context
- Система работает локально (Ollama), без внешних API
- Основные задачи: понять вопрос на русском, найти ответ в базе знаний, ответить на русском
- Генерация: 2-5 предложений, точно по контексту, без галлюцинаций
- Оценка качества (evaluate-узел): LLM оценивает свой ответ 0-100 — нужно понимать инструкции
- RAM ограничен: 8-16 GB типичная машина поддержки

## Research questions

### Q1: Сравнение моделей по качеству на русском (2024-2025)

Найди бенчмарки или обзоры сравнивающие на русскоязычных задачах:
- `mistral:7b` (текущая)
- `llama3.1:8b` (Meta)
- `qwen2.5:7b` (Alibaba — известен качеством на многоязычных задачах)
- `gemma3:9b` (Google, 2025)
- `phi4:14b` (Microsoft, если RAM позволяет)

```
[Таблица: модель | размер | русский score/rank | RAM | скорость tok/s на CPU]
[Источник: ...]
```

### Q2: Какая модель лучше следует инструкциям на русском?

Evaluate-узел просит модель: «Оцени ответ по шкале 0-100, верни ТОЛЬКО число».
Какие модели стабильно возвращают число, а не текст?

```
[Рейтинг по instruction-following на русском: ...]
[Источник: ...]
```

### Q3: Специализированные русскоязычные модели

Есть ли модели, дообученные специально на русском, доступные через Ollama?
- `saiga` (IlyaGusev)
- `vikhr` или другие

```
[Доступны ли через ollama pull? ...]
[Качество vs Qwen2.5/LLaMA3.1: ...]
[Источник: ...]
```

### Q4: Практическая рекомендация для support RAG

Для задачи: «Ответить на вопрос по базе знаний, 2-5 предложений, точно по контексту»
какая модель оптимальна при:
- RAM 8GB: [...]
- RAM 16GB: [...]
- GPU (4-8GB VRAM): [...]

```
[Рекомендация: ...]
[Обоснование: ...]
[Источник: ...]
```

### Q5: Ollama pull commands

Для топ-3 рекомендованных моделей — точные команды:
```
ollama pull [model:tag]
```
И нужно ли менять `OLLAMA_MODEL_NAME` в `.env`?

---

## Output: Recommendation

```
ТЕКУЩИЙ ВЫБОР: mistral:7b
РЕКОМЕНДУЮ ЗАМЕНИТЬ НА: [...]
ПРИЧИНА: [...]
КОМАНДА: ollama pull [...]
ENV: OLLAMA_MODEL_NAME=[...]
```

## CONSTRAINTS
- Только заполнить `[...]`, сохранить как `docs/research/llm-model-selection-2025.md`
- Никаких изменений в коде

## DONE WHEN
- [ ] `docs/research/llm-model-selection-2025.md` существует
- [ ] Есть таблица сравнения минимум 4 моделей
- [ ] Есть конкретная рекомендация с `ollama pull` командой
- [ ] Все `[...]` заполнены
