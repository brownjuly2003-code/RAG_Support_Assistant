"""Sub-routers extracted from api.app.

Each module here exposes a `router: APIRouter` that is included into the
main application router in api.app via `router.include_router(...)`.

Migration tracker: docs of remaining splits live in DEPRECATIONS.md.
"""
